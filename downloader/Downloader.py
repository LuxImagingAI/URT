
class Downloader:
    def __init__(self, dataset, logger, temp_dir, cache_dir) -> None:
        self.dataset = dataset
        self.logger = logger
        self.temp_dir = temp_dir
        self.cache_dir = cache_dir

def run(self):
    raise Exception(f"Run method not implemented")
    

